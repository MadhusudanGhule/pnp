import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Desktop PNP Machine - Professional PCB Assembly Solutions',
  description: 'Professional desktop pick and place machine for PCB assembly and manufacturing. High-precision automated component placement for your production needs.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
